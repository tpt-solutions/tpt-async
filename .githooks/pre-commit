#!/bin/sh
# Install with: git config core.hooksPath .githooks
set -e
echo "[pre-commit] cargo fmt --check"
cargo fmt --all --check
echo "[pre-commit] cargo clippy -D warnings"
cargo clippy --workspace --all-features -- -D warnings -q
